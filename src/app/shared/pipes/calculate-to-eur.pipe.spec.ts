import { CalculateToEurPipe } from './calculate-to-eur.pipe';

describe('CalculateToEurPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateToEurPipe();
    expect(pipe).toBeTruthy();
  });
});
